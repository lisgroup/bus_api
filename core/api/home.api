info(
    desc: "用户中心"
    author: "lisgroup"
    email: "lisgroup@qq.com"
)

type IndexResponse {}
type TokenResponse {}

@server(
    group: home
    prefix: /
)
service core-api {
    @handler Index
    get / returns (IndexResponse)
    @handler Token
    get /token returns (TokenResponse)
}
